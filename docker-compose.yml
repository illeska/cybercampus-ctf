# version: "3.9"

services:
  web:
    build: .
    container_name: cybercampus_web
    ports:
      - "5000:5000"
    env_file:
      - .env
    depends_on:
      - db
    volumes:
      - .:/app
    restart: always

  db:
    image: postgres:15
    container_name: cybercampus_db
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  pgadmin:
    image: dpage/pgadmin4
    container_name: cybercampus_pgadmin
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD}
    ports:
      - "5050:80"
    depends_on:
      - db

  sqli-challenge:
    build: ./challenges/sqli
    container_name: cybercampus_sqli
    ports:
      - "5001:5001"
    restart: always

  xss-challenge:
    build: ./challenges/xss
    container_name: cybercampus_xss
    ports:
      - "5002:5002"
    restart: always

  bruteforce-challenge:
    build: ./challenges/bruteforce
    container_name: cybercampus_bruteforce
    ports:
      - "5004:5004"
    restart: always
    
  crypto-challenge:
    build: ./challenges/crypto
    container_name: cybercampus_crypto
    ports:
      - "5003:5003"
    restart: always

volumes:
  pgdata:
